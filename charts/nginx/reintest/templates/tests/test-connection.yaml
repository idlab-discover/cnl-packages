apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reintest.fullname" . }}-test-connection"
  labels:
    {{- include "reintest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reintest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
